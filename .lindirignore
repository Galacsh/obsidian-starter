# Basically, should ignore all files
**

# But, should track .lindirignore file
!.lindirignore

#############
# .obsidian #
#############

# Should search inside .obsidian folder
!/.obsidian
# Should track all the things inside .obsidian
!/.obsidian/**
# But ignore workspace config
.obsidian/workspace*.json
# But ignore "Iconize" plugin's data
.obsidian/plugins/obsidian-icon-folder/data.json

##########
# config #
##########

# Should search inside config folder
!/config
# Should track all the things inside .obsidian
!/config/**
# But ignore sorting spec
config/sort.md
